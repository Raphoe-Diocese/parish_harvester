# Grok 4.6 run plan — 12/09/2026

Written 12/09/2026 from `docs/AUDIT_2026-09-10.md`, `GAMEPLAN.md`, `docs/WEBSITE_OCR_BACKLOG.md`, `AGENTS.md` and the live site. This is the **only** plan Grok needs. It does not need to ask another agent anything. Frank pastes **one card per chat**, says **go**, checks the proof line, stops.

Order of use: read §0 once, then run the cards in §3 top to bottom. §4 is what has already been done (do not redo). §5 is parked (do not start).

---

## 0. Rules Grok must obey (paste with every card)

**Who you talk to:** Frank. Beginner, product boss, zero budget. Answer shape: Bottom line (1–2 sentences) → What to do (≤ 5 exact clicks) → One warning only if needed → Gameplan this turn (Written down / Done with evidence / Next / Parked). Dates DD/MM/YYYY.

**Truth:** never say done / live / fixed without proof this turn: a live URL you opened, a CI run link, a `parish_status.json` row, or a screenshot Frank can see. Tests or Python-only edits are not proof. If it is not on https://www.parishpress.ie/ say "not live yet". Harvest truth is only `parishes/parish_status.json`. Never invent dates, Mass times, names, OCR text, statuses.

**Scope:** one card per chat, touch only the listed files (max 8), no "while I'm here". Two failed tries → stop and tell Frank what you saw. No `_tmp_*` files in the repo. Do not regenerate or stage all `docs/` pages. Do not run a Full harvest or download mega PDFs on Frank's PC (16 GB laptop, Cursor eats 4.5 GB). Heavy work runs in GitHub Actions or a cloud agent.

**Locked product rules:** mega PDF stays on (`HARVEST_MEGA_PDF=1`); Ghostscript stays `/printer` **150 dpi** (100 dpi was unreadable — never go back); PDF and OCR panels **850px** desktop / **450px** phone (`max-width: 1024px`), content scrolls inside; sticky search outside `#ocr-panel`; `#scroll-top-btn` stays; parish and diocese links `target="_blank" rel="noopener noreferrer"`; never put the mega on iframe `src`; Irish stays Irish; no `continue-on-error` on commit steps.

**Git:** repo `Raphoe-Diocese/parish_harvester`. Frank's PC clone lives at `C:\Users\Digital Admin\repos\parish_harvester` (sparse, blobless — `git sparse-checkout add <folder>` if you need more; **never** use the OneDrive Desktop folders, their `.git` broke on 12/09). Branch `cursor/<card-id>-45f8` from `origin/main`. Commit as `Cursor Agent <cursoragent@cursor.com>`. Push, `gh pr create --base main`. Wait for `test.yml` green. Frank says **merge**, then you `gh pr merge <n> --squash --delete-branch`. Pages deploys on merge (`deploy-pages.yml`); parish/diocese HTML only changes after the next harvest or `ocr-bulletin.yml` run. Windows PowerShell: `;` not `&&`; Python is `C:\Users\Digital Admin\AppData\Local\Programs\Python\Python311\python.exe`; prefix spaced paths with `& "..."`. Write files with a script, not `Set-Content -Encoding UTF8` (adds a BOM).

**Tests that read live data:** `tests/test_ocr_bulletin_pages.py::test_live_*` read `docs/parishes/raphoe/gort-a-choirce.html` and `docs/mega_pdf/*_p1.jpg`; `tests/test_clogher_diocese.py` and `tests/test_train_matching.py` read `parishes/recipes/`. When a harvest or a Frank retrain changes those files, fix the **test**, not the data.

**After every card:** one line under "Audit cards" in `GAMEPLAN.md` (PR number + proof) and the same line in the backlog row if it is a website/OCR card. Tell Frank to copy `GAMEPLAN.md` to `C:\Users\Digital Admin\Dropbox\Cursor gameplans\Harvester\GAMEPLAN.md` if you cannot write there.

---

## 1. Where the product is (12/09/2026)

- Harvest week **06/09/2026**, last Full harvest 10/09 night: **ok 108 / 164**, actionable 24. A harvest also ran 12/09 18:25 (megas `Last-Modified 12 Sep 18:26`). Live megas: Raphoe **6.98 MB**, Down & Connor **16.5 MB** (150 dpi live since 11/09 — Frank confirmed sharper 12/09).
- Live site https://www.parishpress.ie/: 4 diocese viewers (Raphoe, Derry, Clogher, Down & Connor), OCR + search, 850/450 panels measured live 10/09. 22 other dioceses are 1.5 KB stubs.
- Phone cache stamp `?h=` live (Frank confirmed 12/09).
- Trainer 1.61.24 installs by zip → Extract → Load unpacked. Auto-update is dropped.
- **#205 merged** `437b4c0c` + Pages 34721204022. Live `?v=20260912a`. Frank phone yes 12/09 23:50.
- **#204** A5 stale caption — rebased onto main 12/09 night (cherry-pick `a943a15`). Not live until merge + next OCR.

## 2. What is broken or lying (proved)

1. Diocese PDF box — **done live** (#205, Frank phone 12/09).
2. Leftover parish pages label a stale bulletin as "this week" (Ardara) — A5, PR #204.
3. Last-week pages sit inside this week's mega (Ardara, Inver, Bruckless) — undated URLs stay `ok` (backlog 06/09).
4. Trainer buttons that lie: "✅ PDF is correct — done" records nothing; "🗑️ Mark as Dead Website" always fails (C1).
5. No CI guard for 850/450 / sticky search / new-tab links on generated HTML (B3). Harvest tests are `continue-on-error` (B4).
6. `deploy-pages.yml` picks the mega by smaller size, can pin an old file (F1).
7. Paid OCR vision runs on the whole mega before the embedded-text preference (E1). Stub OCR commits silently (E2).
8. 8 of 116 parish PDFs are 1.7 KB "slice unavailable" placeholders while the row says `ok` (backlog 24/08).
9. `docs/search-index.json` stale; `docs/calendars/*.ics` are empty shells; disabled parishes still get external links in the A–Z grid (backlog 23/08 rows).
10. Bruckless must be typed as `drive-1rjeey-ayy` in Harvest; Drive `/folders/` listing can be `ok` (GAMEPLAN 10/09).
11. Stranorlar recipe 404s on a predicted `…/2026/09/6th-Se…` URL (GAMEPLAN 11/09).
12. 11 competing markdown plans; README describes dead features (D1, D2).

---

## 3. Cards — run in this order

Sizes: **match** = 1–2 files, under an hour, Grok 4.6 low. **candle** = ≤ 4 files, half a day, Grok 4.6 medium. Every card: branch, listed files only, listed tests, PR, wait green, Frank says merge, proof line, one GAMEPLAN line.

### P1 · Merge #205 and prove the PDF counter — match
- Goal: [PR #205](https://github.com/Raphoe-Diocese/parish_harvester/pull/205) green → Frank says **merge** → Pages deploy → live.
- Files: none (merge only). If CI is red on a data test, fix that test only.
- Proof: `curl -sL https://www.parishpress.ie/dioceses/raphoe/ | grep -o 'pdf-inpage-viewer.js?v=[0-9a-z]*'` prints `?v=20260912a`; Frank Ctrl+F5 on the phone and sees "Loading PDF… X of Y MB" then "✅ PDF fully loaded — N pages". Tick backlog row 2026-09-12 (diocese PDF) only after Frank's phone.

### P2 · Rebase and merge #204 (A5 stale caption) — match
- Goal: `git fetch; git rebase origin/main` on `cursor/a5-stale-parish-caption-45f8`, push `--force-with-lease`, CI green, Frank says merge.
- Files: none new. Tests: `pytest tests/test_parish_pages.py -q`.
- Proof: after the next `ocr-bulletin.yml` run, https://www.parishpress.ie/parishes/raphoe/ardara.html shows "Latest bulletin we have: DD/MM/YYYY (not this week)" + `.stale-note` (only if Ardara is still stale in `parish_status.json`). Screenshot.

### B3 · Generated-HTML guard test for the locked viewer rules — candle
- Goal: test renders one diocese page + one parish page via `render_bulletin_viewer_shell` / `site_builder` into a temp dir and asserts: `850px` height/min/max on `#ocr-panel` and the PDF wrap; `450px` inside `@media (max-width: 1024px)`; `.ocr-sticky-chrome` before `#ocr-panel`; `id="scroll-top-btn"`; parish/diocese links `target="_blank" rel="noopener noreferrer"`; `docs/assets/pdf-inpage-viewer.js` `ensureStyles()` has `850px!important` and `450px!important`; `WHOLE_FILE_MAX` ≥ 24 MB; the string `PDF fully loaded`.
- Files: new `tests/test_viewer_rules.py` (+ `tests/test_site_builder.py` if a helper is needed). Max 2.
- Proof: test on `main`; green `test.yml` link.

### C1 · Kill the two lying Trainer buttons — match
- Goal: "✅ PDF is correct — done" says "Noted — refresh Problems to check GitHub" (or is removed); "🗑️ Mark as Dead Website" overlay removed (working path is `mark_parish_dead` in the Problems tab).
- Files: `extension/content.js`, `extension/manifest.json` (bump 1.61.25), `tests/test_send_test_comms.py`, `tests/test_extension_messaging.py` (version pins). Max 4.
- Tests: `pytest tests/test_extension_messaging.py tests/test_send_test_comms.py -q`; `node --check extension/content.js`.
- Proof: https://www.parishpress.ie/extension/parish_trainer.zip `manifest.json` shows 1.61.25; Frank runs `scripts/refresh_local_trainer.ps1` then Reload at chrome://extensions; card shows 1.61.25. Screenshot.

### F1 · Pick the mega by date, not size — match
- Goal: `deploy-pages.yml` "keep smaller committed mega" compares `.pages.json` `date`; newer wins; size only as tie-break.
- Files: `.github/workflows/deploy-pages.yml`. Max 1.
- Proof: after the next harvest `curl -sI https://www.parishpress.ie/mega_pdf/raphoe_mega_bulletin.pdf` `last-modified` = harvest day and `.pages.json` `date` = target Sunday.

### H1 · Undated bulletin URL is never `ok` when the mega still shows last week — candle
- Goal: close backlog 06/09 row. In `harvester/bulletin_freshness.py`, when the URL/filename has no date and the PDF's own text has no this-week date, the outcome is `stale` (with `bulletin_date` from the PDF text if found), not `ok`. Ardara `sun-30th-august-26`, Inver `sat_29th__-_sun_30th__august_2026` must parse as 30/08.
- Files: `harvester/bulletin_freshness.py`, `harvester/utils.py`, `tests/test_bulletin_freshness.py`. Max 3.
- Tests: `pytest tests/test_bulletin_freshness.py tests/test_parish_status.py -q`.
- Proof: next Sunday `parish_status.json` on `main`: Ardara/Inver rows `stale` with `bulletin_date` = their real week; live Raphoe mega has no 30/08 pages (Frank checks Jump-to Ardara).

### H2 · Bruckless by name; Drive folder listing never `ok` — candle
- Goal: `main.py --target-parish bruckless` resolves to `drive-1rjeey-ayy` via `HARVEST_INPUT_ALIASES` in `harvester/parish_aliases.py` (not a skip-alias; do not invent `parishes/parish_aliases.json`). Harvest workflow input accepts the name. A Drive `/folders/` URL as final download = `failed` with error "Drive folder listing, not a file".
- Files: `main.py`, `harvester/parish_aliases.py`, `harvester/parish_status.py`, `tests/test_parish_aliases.py` (+ status test). Max 5.
- Tests: `pytest tests/test_parish_aliases.py tests/test_parish_status.py -q`.
- Proof: `gh workflow run harvest.yml -f target_parish=bruckless -f diocese=raphoe` succeeds; run link; `parish_status.json` row for `drive-1rjeey-ayy` updated `last_tested_at`. **Not live / not harvested until Frank says go on that run.**

### H3 · Stranorlar recipe — match (only if a this-week file exists)
- Goal: open https://stranorlarparish.ie (see `parishes/recipes/raphoe/stranorlarparish.json` `start_url`). If a **this-week** PDF is on the site (HTTP 200, `%PDF`), fix the picker (newest_dated on the listing, not a predicted `/2026/09/6th-Se…` URL). If no this-week file exists, set `harvest_note` "No new bulletin on the site since 28/06/2026" and stop.
- Files: `parishes/recipes/raphoe/stranorlarparish.json`, `parishes/training_diagnosis/stranorlarparish.json`. Max 2.
- Proof: single-parish harvest run link → `parish_status.json` row `ok` with `bulletin_date` this week, **or** row `error` starting with the note.

### C2 · Remove auto-update plumbing — candle
- Goal: remove `update_url` and `contextMenus` from `manifest.json`; delete `updates.xml`, `scripts/write_updates_xml.py`, `scripts/pack_extension_crx.py`, `docs/extension/parish_trainer.crx`, `docs/AUTO_UPDATE_SETUP.md`; drop the CRX gate + XML steps in `deploy-pages.yml`; drop the `updates.xml` assert in `tests/test_train_matching.py`. Keep zip build and `key`.
- Files: the 8 above + manifest bump + 2 test pins. Max 11 (all listed).
- Tests: `pytest tests -q -k "train_matching or extension or send_test"`; `node --check extension/*.js`.
- Proof: `deploy-pages.yml` green after merge; https://www.parishpress.ie/updates.xml → 404; zip installs; card shows new version.

### B4 · Harvest tests block; tests run on code PRs — candle
- Goal: `harvest.yml` test step loses `continue-on-error`; `test.yml` installs `pymupdf`. Keep `[skip ci]` on data commits.
- Files: `.github/workflows/harvest.yml`, `.github/workflows/test.yml`, `requirements.txt` if needed. Max 3.
- Warning: run the full suite twice in a cloud agent first and report the count. A flaky test here blocks Sunday.
- Proof: green `test.yml`; next harvest run shows the test step required.

### C3 · Truthful GitHub errors and one poller — candle
- Goal: 403 with `X-RateLimit-Remaining: 0` → "GitHub rate limit — try again in N min"; one poller per dispatch; `locateRecipe` treats timeout/5xx as error not "not found".
- Files: `extension/github_recipe_push.js`, `extension/content.js`, `extension/sidepanel.js`, manifest bump + 2 pins. Max 6.
- Proof: Send & test on one parish; DevTools Network shows one polling stream; new version on the card.

### C4 · Delete dead extension code — match
- Goal: remove unused handlers/functions listed in `docs/AUDIT_2026-09-10.md` card C4.
- Files: `background.js`, `sidepanel.js`, `content.js`, manifest bump + 2 pins. Max 6.
- Proof: `rg` shows zero references; zip live with new version; Problems tab still lists actionable parishes.

### S1 · Placeholder slices are not `ok` — candle
- Goal: when a parish's page range is not found in the mega `.pages.json`, write `slice_missing: true` on its `parish_status.json` row and make the parish page say "This week's PDF page for X could not be cut from the collated bulletin" instead of a 1.7 KB stub that looks like a PDF. Homepage ready count excludes `slice_missing`.
- Files: `ocr/parish_pages.py`, `harvester/parish_status.py`, `harvester/site_builder.py`, one test. Max 4.
- Proof: next OCR run — the 8 listed keys (castleblayney, clones, clontibret, tyholland, parishofballinascreen, kilmoreandkillyleagh, saintanthony, kincasslagh) show the honest line live; `parish_status.json` has `slice_missing`.

### S2 · A–Z grid: no external link for `disabled` parishes — match
- Goal: `render_parish_link_grid` does not fall back to an external href when the parish_status outcome is `disabled` (Greencastle 502 affiliate redirect, Coleraine, Long Tower).
- Files: `harvester/site_builder.py`, `tests/test_site_builder.py`. Max 2.
- Proof: after the next harvest, https://www.parishpress.ie/dioceses/derry/ has no `href="https://greencastleparish.com/"`.

### S3 · Search index and calendars: fix or stop publishing — candle
- Goal: Frank decides (default = **stop publishing**): remove `/search/`, `/calendars/`, `/feeds/`, `/badges/`, `/subscribe/` links and generators, or fix `manifest_builder.py` (`VIEWER_FILE_PATTERN` add raphoe, `PARISH_NAME_PATTERN` → `ocr-parish-masthead`) and stage `docs/search-index.json` in `harvest.yml`. Also delete dead `write_root_index()` in `ocr/generate_bulletin_pages.py` and the stub `docs/bulletins/raphoe/index.html`.
- Files: ≤ 6, listed in the PR.
- Proof: https://www.parishpress.ie/search/ either returns useful results for "Annagry" or 404 with no inbound link on the homepage.

### D1 · One truth in markdown — candle
- Goal: delete `CURSOR_NEXT_TASK.md`, `CURSOR_LAST_RESULT.md`, `PROJECT_PLAN.md`, `CHECKPOINTS.md`, `FREE_SOLUTION.md`, `SITE_MAP.md`, `ai/README.md`, `docs/COST_DASHBOARD.md`, `docs/EMBEDDING.md`, `docs/AUTO_UPDATE_SETUP.md`; merge `DECISIONS_LOG.md` rules into `AGENTS.md`; merge the three domain docs into `docs/DOMAIN.md`; fix owner to `Raphoe-Diocese` everywhere; fix `SECURITY.md` public-key wording. Keep `AGENTS.md`, `GAMEPLAN.md`, `docs/WEBSITE_OCR_BACKLOG.md`, `docs/AUDIT_2026-09-10.md`, this file, `docs/DIOCESE_HUNT.md`.
- Proof: `*.md` count ≤ 20; `rg -l "frankytyrone.github.io|Frankytyrone/parish_harvester"` empty outside `docs/audit/`.

### D2 · README and WHAT_IS_THIS say what exists — match
- Files: `README.md`, `WHAT_IS_THIS.md`, `CHANGELOG.md` (resume at 1.61.x or delete). Max 3.
- Proof: `rg -n "summary|calendar|scheduler|github.io" README.md WHAT_IS_THIS.md` empty.

### E1 · Vision only on sparse pages — candle
- Goal: `ocr/convert_bulletin.py main()` sends only sparse/image pages to Mistral/Gemini; tier order embedded text → sparse pages to vision → stub. Test with a fake provider.
- Files: `ocr/convert_bulletin.py`, `ocr/sparse_page_ocr.py`, one test. Max 3.
- Proof: next `ocr-bulletin.yml` log "N sparse pages sent to vision" with N < total; Gortahork Irish still Irish live.

### E2 · OCR stub must not look like success — match
- Files: `ocr/convert_bulletin.py`, `.github/workflows/ocr-bulletin.yml`. Max 2.
- Proof: YAML + code on `main`; next run green or red with a visible `::error`.

### F2 · Standalone `-ocr.html` pages: document as exempt — match
- Files: `docs/WEBSITE_OCR_BACKLOG.md` locked rules (default) . Max 1.
- Proof: the line on `main`.

### F3 · Backlog reconciliation — match
- Goal: the ~40 `doing`/`todo` rows from 26/08–12/09 in `docs/WEBSITE_OCR_BACKLOG.md`: mark `done` only where a live check in this plan or the audit proves it; otherwise leave `doing` and add "needs Frank's phone". Cite the proof in each changed row.
- Files: `docs/WEBSITE_OCR_BACKLOG.md`. Max 1.
- Proof: the diff.

### C5 · One executable JS test — candle
- Goal: Node test in `test.yml` loading `extension/github_recipe_push.js` with stubbed `fetch`; asserts `pollHarvestUntilDone` ok only when `last_tested_at` changes; every `chrome.runtime.sendMessage({type})` in `content.js`/`sidepanel.js` has a handler in `background.js`.
- Files: `tests/js/` (≤ 3 files), `.github/workflows/test.yml`. Max 4.
- Proof: green `test.yml` with the Node step.

### M1 · Mega bytes (only if Frank still says slow after P1) — match
- Goal: keep 150 dpi; try `-dPDFSETTINGS=/ebook` JPEG quality **at 150 dpi** (not the 100 dpi default) in `harvester/pdf_compress.py`, plus `-dColorConversionStrategy=/sRGB`. Measure Down & Connor before/after in the harvest log.
- Files: `harvester/pdf_compress.py`, `tests/test_pdf_compress.py`. Max 2.
- Proof: `curl -sI …/down_and_connor_mega_bulletin.pdf` content-length drops ≥ 30 % **and** Frank says the Annagry page is still readable on the phone. If not readable, revert.

---

## 4. Already done — do not redo

- A2 (#193), B1 (#194), B2 (#195), A1 (#196), A3 (#197), 7 `no_evidence` parishes (#198), A4 (#199), Trainer folder refresh script (#201), Bruckless Drive year folder (#202), 150 dpi megas + `?h=` phone cache stamp (#203, live, Frank confirmed 12/09).
- PDF loading counter + "PDF fully loaded" + 24 MB single GET + viewer `?v=20260912a` + Gortahork test unpin + Tyholland/Galloon test update: **PR #205** merged; Frank phone yes 12/09.
- A5 stale caption: **PR #206** merged (old #204 closed). Not live until next harvest/OCR.
- B3 viewer guard: **PR #207** merged. C1 Trainer buttons: **PR #208** merged. F1 mega by date: **PR #210** merged.
- H1 date parse for Ardara/Inver already on main. H2 Bruckless-by-name: this turn.

## 5. Parked — do not start unless Frank says go

- Recipe Brain / Referee (Phase 4). Site look rewrite. 22 stub dioceses (`docs/DIOCESE_HUNT.md`). Saint Malachy's 7-page cap. Uploader zip on newtownkilleaparish.ie (Frank does it). Remove bulletin archive nav. Split `content.js` (9,486 lines), retire `fetcher.py`, split `replay.py` (bonfires G1–G3). Git history rewrite, 47 stale branches. Gortahork page trailing next-parish header (cosmetic). Moving Frank's working folder off OneDrive permanently (the clone in `C:\Users\Digital Admin\repos\` already does this for agents).

## 6. Expected finish

When P1–F3 are proved live: harvest labels honest, viewer counter live, CI enforces the locked rules, Trainer has no lying buttons, one plan. Re-score against `docs/AUDIT_2026-09-10.md` §2 — expect ~80/100. The rest is blocked-network recipes and the 22 dioceses.
