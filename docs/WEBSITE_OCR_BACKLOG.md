# Website / OCR living backlog

**Read this file before any website, OCR, diocese viewer, parish page, or mega-PDF viewer work.**

This is the single list. Do not create a second competing plan. Do not mark an item **done** unless you have verified it on **generated** HTML/CSS (the files under `docs/dioceses/`, `docs/parishes/`, `docs/bulletins/`, or a fresh `render_bulletin_viewer_shell` output) — changing Python alone is not enough. Harvest regenerate overwrites live HTML from the canonical generator.

**Do not lie about done.** Never tell Frank a website/OCR/viewer job is finished unless you have checked the **generated files that GitHub Pages deploys** (`docs/index.html`, `docs/dioceses/`, `docs/parishes/`) AND, after merge+Pages, the **live URL** (e.g. https://www.parishpress.ie/). Tests or Python-only edits are not enough. If it is not on the live page, say “not live yet”. Do not tick backlog items done without the live check.

Date format for new rows: `YYYY-MM-DD`. User-facing dates on the site stay **DD/MM/YYYY**.

## Locked product rules (do not “helpfully” undo)

| Rule | Where it lives |
|------|----------------|
| Mega PDF generation stays **on** (`HARVEST_MEGA_PDF=1`) | `.github/workflows/harvest.yml`, `AGENTS.md`, `DECISIONS_LOG.md` |
| Desktop PDF **and** OCR panels: **locked** `height` / `min-height` / `max-height` **850px**; extra content scrolls **inside** the box (`overflow: auto`). Not `height: auto`. Not `85vh`. Frank 27/08/2026: desktop mega PDF must be one 850px window, not every bulletin down the page (overrides 22/08 page-scroll). Phone box **450px**. Do not use Chrome’s PDF iframe (Page X of Y). | `ocr/generate_bulletin_pages.py` → `render_bulletin_viewer_shell`, `pdf_inpage_viewer_css()`; `docs/assets/pdf-inpage-viewer.js` `ensureStyles()` |
| Mobile/tablet (max-width 1024px): **locked 450px** (height + min + max), inner scroll — but a **desktop window narrower than 1025px is still desktop**: `@media (min-width: 701px) and (min-height: 501px)` puts 850px back with `!important`. Do not delete that block. | same function + JS, `@media (max-width: 1024px)` then `desktop_viewer_height_lock_css()` |
| Sticky OCR search stays **outside** `#ocr-panel`; page **↑** `#scroll-top-btn` shows when the **inner** PDF/OCR box or the page is scrolled, and jumps those boxes **and** the page to the top | `harvester/site_chrome.py` `scroll_top_js`, viewer shell, `docs/assets/pdf-inpage-viewer.js` `ensureScrollTop` |
| Parish / diocese outbound links open in a **new tab** (`target="_blank"` `rel="noopener noreferrer"`) | viewer shell, parish grids, `docs/assets/pdf-inpage-viewer.js` |
| OCR shows a **parish name header**, real **section headings**, professional spacing | `ocr/bulletin_layout.py`, `ocr_reading_css()` |
| Do not invent events or translate Irish/Gaeilge | `ocr/bulletin_layout.py`, `ocr/convert_bulletin.py` prompt |
| Keep real parish PDF slices (do not revert parish pages to fake mega-only PDFs) | `ocr/parish_pages.py` |
| **Do not lie about done.** Generated Pages files + live URL after merge+Pages. If it is not on the live page, say “not live yet”. | `AGENTS.md`, this file, `.cursor/rules/website-ocr-backlog.mdc` |

## How to close an item

1. Change the **canonical generator** (not only a one-off HTML file).
2. Regenerate the live diocese pages (Raphoe, Derry, Down & Connor) so parishpress.ie gets the fix after deploy.
3. Check the **generated files GitHub Pages deploys** (`docs/index.html`, `docs/dioceses/`, `docs/parishes/`).
4. After merge+Pages, check the **live URL**. If it is not on the live page, say “not live yet”. Do not tick done without that live check.
5. Tick the box, set status to `done`, and note the PR / date verified.

---

## Current checklist

| Status | Added | Item | Where in the code |
|--------|--------|------|-------------------|
| done | 2026-08-20 | Living backlog file (this document) so website/OCR requests cannot be memory-holed | `docs/WEBSITE_OCR_BACKLOG.md` |
| done | 2026-08-20 | Cursor rule: read this file first; 850/450; new-tab links; mega PDF stays on | `.cursor/rules/website-ocr-backlog.mdc` |
| done | 2026-08-20 | Short pointer in the agent guide | `AGENTS.md` (Website / OCR backlog) |
| done | 2026-08-20 | Visible **parish name header** on every parish OCR block (name + date if known). Not an accordion. | `ocr/bulletin_layout.py` (`render_parish_masthead`, `structure_ocr_html`); CSS in `ocr_reading_css()` |
| done | 2026-08-20 | Promote existing bulletin topic lines to real `h2`/`h3` (Mass times, Anniversaries / RIP / deceased, Community / notices, Fundraising / bingo / events, Contact). Do not invent text. Keep Irish as Irish. | `ocr/bulletin_layout.py`; applied from `ocr/convert_bulletin.py` (`render_markdown_lines`) and `prepare_ocr_fragment` |
| done | 2026-08-20 | Readable OCR measure: larger body type, line-height ~1.65, space after headings, max-width ~72ch, soft stone paper (not harsh black-on-white), mobile wrap | `ocr_reading_css()` in `ocr/generate_bulletin_pages.py`; mirrored in `ocr/convert_bulletin.py` `CSS` |
| done | 2026-08-20 | Mega PDF parish URL links at the top of each parish must **work** and open in a **new tab** (PDF.js was painting canvases only, so annotations were dead) | `harvester/stitcher.py` (`_build_parish_header_pdf` `linkURL` + `newWindow=True`); `docs/assets/pdf-inpage-viewer.js` annotation overlay |
| done | 2026-08-20 | Desktop 850px / mobile 450px for PDF **and** OCR in the canonical generator. Regenerated live diocese HTML so harvest cannot drop it. Later locked as **fixed** visible boxes (not min-height-only growth) — see 2026-08-22 item. | `render_bulletin_viewer_shell`; tests in `tests/test_ocr_bulletin_pages.py`, `tests/test_page_renderer.py`, `tests/test_site_builder.py` |
| done | 2026-08-20 | Parish / diocese HTML links already use `target="_blank"` `rel="noopener noreferrer"` | `render_bulletin_viewer_shell`, `render_parish_link_grid`, `ocr/parish_pages.py` |
| locked | 2026-08-20 | Mega PDF generation stays on — do not disable `HARVEST_MEGA_PDF` | `.github/workflows/harvest.yml`, `main.py` (single-parish skip is intentional) |

Status values: `todo` · `doing` · `done` · `locked` (must not be undone) · `parked`

---

## Still open (do not pretend these are finished)

- [ ] **doing** · 2026-08-27 · Delete **Tap to enlarge** (broken, not needed). Frank 27/08. Do not mark done until live Raphoe JUMP TO row has no enlarge button. · `ocr/generate_bulletin_pages.py`, `docs/assets/pdf-inpage-viewer.js`

- [ ] **doing** · 2026-08-27 · Desktop PDF must be an **850px framed window** (`.pdf-frame-wrap` / `.pdf-inpage-viewer` `height`/`max-height` 850px, `overflow: hidden`, 1px border), inner `.pdf-inpage-pages` scroll. Frank 27/08 18:58: the 850px iframe is not there — wrap was `height:auto` / `overflow:visible` so the page dumped. This turn locks wrap+viewer in `ensureStyles()` + generator CSS and cache-busts the four live diocese pins to `?v=20260827d`. Do not mark done until live Raphoe desktop (Ctrl+F5 after Pages) shows one framed 850px window. · `render_bulletin_viewer_shell`, `docs/assets/pdf-inpage-viewer.js`, `docs/dioceses/{raphoe,derry,down-and-connor,clogher}/index.html`

- [ ] **rejected** · 2026-08-27 · PR [#149](https://github.com/Raphoe-Diocese/parish_harvester/pull/149) same-tab native PDF link (`fillPhoneNativeLink` / `pdf-phone-native` / **This week's PDF**) whenever `isMobileView()`. Frank rejected it: the tap to find the PDF does not work. Proved: `jumpPdf()` needs `.pdf-inpage-pages` and `[data-page]` slots; `fillPhoneNativeLink` did `wrap.innerHTML = ""` so Jump-to silently failed. · `docs/assets/pdf-inpage-viewer.js`

- [ ] **doing** · 2026-08-27 · Restore phone PDF.js so Jump to works. Phone and desktop both `loadPdfJs` + `startViewer` (self-hosted `/assets/pdf.min.js`, stream/Range, `rangeChunkSize` 262144). Phone `pageWidth` is `Math.max(boxWidth, 720)` with `overflow-x: auto` and dpr cap 2 (not 1). Do not wipe `.pdf-inpage-pages`. Locked 450/850 stay. Viewer `?v=20260827g`. Do not mark done — not live until Pages; not phone-tested. · `docs/assets/pdf-inpage-viewer.js`, `ocr/generate_bulletin_pages.py`

- [ ] **doing** · 2026-08-27 · Delete **Show PDF** / `armPhoneTapToLoad` / `data-pdf-tapped`. The in-page viewer starts on load. PDF.js **3.11.174** is self-hosted at `/assets/pdf.min.js` + `/assets/pdf.worker.min.js` (not cdnjs after tap). `rangeChunkSize` **262144**; first `getDocument` streams, retry stream once, then full-file fallback. PDF and OCR boxes locked **850px** PC / **450px** phone (`ocr/generate_bulletin_pages.py` + `ensureStyles()`). Viewer `?v=20260827g`. Do not mark done — not live until Pages; not phone-tested. · `docs/assets/pdf-inpage-viewer.js`, `ocr/generate_bulletin_pages.py`

- [ ] **doing** · 2026-08-27 · Keep small megas from now on. Sunday harvest install must include Ghostscript (`gs --version` fails the step if missing). OCR copy and Pages OCR snapshot must not replace a smaller committed `docs/mega_pdf/*_mega_bulletin.pdf` with a fatter staged/artifact file. Keep the #140 committed-wins skip (`Using committed docs/mega_pdf — skip stale harvest artifacts`) and docs/mega_pdf overwrite-last in Verify. Mega PDF generation stays on. Do not mark done until a harvest + OCR + Pages cycle leaves the small files (Raphoe ~6.1M, not 17MB). · `.github/workflows/harvest.yml`, `.github/workflows/ocr-bulletin.yml`, `.github/workflows/deploy-pages.yml`

- [ ] **doing** · 2026-08-26 · Phone Open PDF + gentle mega compress. Homepage Mega PDF is same-tab (no `target=_blank`) — that is the good path. Viewer JS this turn `?v=20260827g`. Phone Open bulletin keeps in-page PDF.js (PR #149 native-link **rejected**). Desktop wrap is a locked 850px window. Phones strip `download`/`target` from mega PDF links and rename Download → Open PDF. Harvest runs Ghostscript `/ebook` after stitch (`harvester/pdf_compress.py`). **Smaller mega bytes are live** (Raphoe ~5.55 MB linearized). Leave open until a phone Open PDF is watched staying in the same tab. · `docs/assets/pdf-inpage-viewer.js`, `harvester/pdf_compress.py`, `harvester/stitcher.py`, `docs/index.html`, `docs/mega_pdf/`

- [x] **done** · 2026-08-26 · Pages still served the **old 16–18 MB** mega PDFs after PR [#139](https://github.com/Raphoe-Diocese/parish_harvester/pull/139) (committed Raphoe is 6,383,586). Cause: `deploy-pages.yml` copied fat harvest artifacts first, then skipped `docs/mega_pdf` when dest already existed. Fix: skip the harvest-artifact fallback when committed `docs/mega_pdf/*_mega_bulletin.pdf` exists, and always copy committed files last. **Live-proved 26/08/2026** after PR [#140](https://github.com/Raphoe-Diocese/parish_harvester/pull/140) squash-merge `a1682ec4` + Pages run [33021470980](https://github.com/Raphoe-Diocese/parish_harvester/actions/runs/33021470980) success. Deploy log: `Using committed docs/mega_pdf — skip stale harvest artifacts`, staged Raphoe 6.1M, `HAD_NEW_ARTIFACTS=false`. HEAD https://www.parishpress.ie/mega_pdf/raphoe_mega_bulletin.pdf HTTP 200 `content-length: 6383586` `Last-Modified: Wed, 26 Aug 2026 22:57:50 GMT` (was 17,525,750 before). Derry 6,407,081 · Down & Connor 7,844,961 · Clogher 3,810,520. · `.github/workflows/deploy-pages.yml`
- [ ] **todo** · 2026-08-24 · Single-parish OCR pages can trail the **next** parish's header. https://www.parishpress.ie/parishes/raphoe/gort-a-choirce.html ends with a bare `Inver` name + date after the Gortahork text. The diocese panel is now sliced on stitcher page boundaries (PR [#124](https://github.com/Raphoe-Diocese/parish_harvester/pull/124)); the per-parish page still keeps a trailing header from the following page. Small and cosmetic — do not reopen the header work for it. · `ocr/parish_pages.py`, `ocr/parish_splitter.py`

- [x] **done** · 2026-08-24 · Gortahork (`gort-a-choirce`) OCR was empty (banner-only mega page, Irish image body never OCR'd). **Live-proved 24/08/2026** on https://www.parishpress.ie/parishes/raphoe/gort-a-choirce.html — body reads `POBAL CHRIOST RI` / `An tAth. Donnchadh Ó Baoill` / `AIFRINN NA SEACHTAINE` / `22.08 Satharn 7 i.n. Tom Dooley,Gort a' Choirce` / `BÁS LE GAIRID` / `CUIMHNEACHÁIN BÁIS`. Irish kept as Irish (`Ní bheidh aifreann`, `Buíochas don phobal`). Note the parish's own printed date is `16ú Lúnasa 2026` — that is a staleness question for the recipe row, not OCR. · `ocr/sparse_page_ocr.py`, `ocr/parish_splitter.py`, `ocr/parish_pages.py`
- [x] **done** · 2026-08-22 · Desktop 850px is a **fixed** visible box: `.pdf-inpage-pages` and `#ocr-panel` use `height: 850px; min-height: 850px; max-height: 850px; overflow: auto`. Extra PDF pages / OCR text scroll **inside** the box. Mobile/tablet `max-width: 1024px`: locked 450px the same way. Not `height: auto`, not `overflow: visible`, not `85vh`. Sticky search stays **outside** `#ocr-panel` (`.ocr-sticky-chrome`). Page **↑** `#scroll-top-btn` still jumps to the top of the page. Runtime `ensureStyles()` locks the same sizes with `!important` + cache-bust `?v=20260822c`. Verified on generated `docs/dioceses/raphoe/index.html`, `docs/dioceses/down-and-connor/index.html`, `docs/parishes/raphoe/gort-a-choirce.html`. · `render_bulletin_viewer_shell`, `pdf_inpage_viewer_css`, `docs/assets/pdf-inpage-viewer.js`
- [ ] **doing** · 2026-08-21 · Back room **View bulletin** opens this week’s scraped PDF: `Bulletins/<key>.pdf`, then `current/`/`stale/`, then the parishpress parish slice. No zip archives. Ship as Parish Trainer v1.61.12. · `extension/sidepanel.js`, `harvester/report.py`
- [ ] **doing** · 2026-08-21 · Trainer Guess must show the guessed URL + title and a **Save guessed link** / **Use this link** control that writes a real recipe step (download / goto / newest-picker). Top 3 candidates. Prefer newest Sunday; skip GDPR / privacy / wedding / Order of Mass. Do not hide Guess after refresh. Manifest footer must match. · `extension/content.js`, `extension/copilot.js`, `extension/manifest.json`
- [ ] **doing** · 2026-08-21 · Trainer ↔ GitHub sync: Problems/Directory load latest `parishes/parish_status.json` via commit SHA; Directory shows ok + UK date; Send & test waits for `last_tested_at` change · `extension/*`
- [ ] **doing** · 2026-08-22 · Add Clogher Diocese from official Expand-all directory (37 parishes; 21 harvestable after kitchen-sink + snapshot). Facebook stays clickable. Hunt command is `docs/DIOCESE_HUNT.md`. Do not mark the public Clogher page done until a harvest writes `docs/dioceses/clogher/` · `parishes/clogher_diocese_*`, `parishes/recipes/clogher/`, `parishes/dioceses.json`
- [x] **done** · 2026-08-22 · Sticky OCR search (`.ocr-sticky-chrome { position: sticky; top: 0 }`) and `#scroll-top-btn` / `.scroll-top-btn` page-top jump. Search highlight uses `scrollIntoView({ block: 'start' })` so it sits below the sticky bar. If a parish page is missing the wrapper, `pdf-inpage-viewer.js` wraps zoom + search + tools. Verified on generated Raphoe diocese + Gortahork parish HTML. · `harvester/site_chrome.py`, `ocr/generate_bulletin_pages.py`, `docs/assets/pdf-inpage-viewer.js`
- [x] **done** · 2026-08-22 · Homepage: one compact row of live dioceses with cathedral photos, short welcome, no junk footer. Live-checked 22/08/2026 on https://www.parishpress.ie/ after PR #83 + Pages run 32603184327. · `harvester/site_builder.py` `_landing_page`
- [x] **done** · 2026-08-22 · Favicon on generated heads (`href="/favicon.png"` + apple-touch). Live-checked 22/08/2026: https://www.parishpress.ie/favicon.png HTTP 200 `image/png` 6867 bytes; homepage HTML has `href="/favicon.png"`. PR #83. · `harvester/site_chrome.py` `favicon_link_tags`, `harvester/site_builder.py`, `ocr/generate_bulletin_pages.py`
- [x] **done** · 2026-08-22 · Live diocese cards: one-line names (`Down & Connor Diocese`, `white-space: nowrap`, shrink `is-long` / `is-very-long`). Live-checked 22/08/2026 on https://www.parishpress.ie/: HTML has `Down &amp; Connor Diocese`, `white-space: nowrap`, `is-long`; does **not** have wrapping `Down and Connor Diocese`. PR #83. · `harvester/site_builder.py` `_live_card_heading_html`
- [x] **done** · 2026-08-22 · Homepage Clogher card: status eyebrow stays on one line (`No data yet` + `.live-card-eyebrow { white-space: nowrap }`). Verified live 23/08/2026 on https://www.parishpress.ie/ after PR #85 + Pages run 32606594172. Old wrapping text is gone; more-dioceses note unchanged · harvester/site_builder.py _status_label
- [ ] **doing** · 2026-08-23 · Killanny HTML /parish-bulletin harvested as stale (May 2021 text still live 23/08/2026). Do not skip; do not invent an August PDF. Leave open until a harvest writes parish_status for killanny · parishes/recipes/clogher/killanny.json
- [ ] **doing** · 2026-08-23 · Bundoran (Magh Ene / Clogher) content gap — **no this-week file**. Proved 23/08/2026 on magheneparish.ie: newest real newsletter is `https://magheneparish.ie/wp-content/uploads/2025/02/Parish_Newsletter_09.02.2025.pdf` HTTP 200 `application/pdf` 1029778 bytes, Last-Modified Sat 08 Feb 2025, PDF CreationDate `20250208102942`. Listing https://magheneparish.ie/newsletter/ HTTP 200 embeds that Feb 2025 file (page last modified 2025-02-08). Harvest mixed URL `http://magheneparish.ie/wp-content/uploads/2026/08/Parish_Newsletter_09.02.2025.pdf` is HTTP 404 this turn. `/wp-content/uploads/2026/08/` exists but is empty (month folder only). WP media after 2025-02-08: 0 items. Do not invent an August 2026 PDF. Do not change the recipe until a new file is on the site. · parishes/recipes/clogher/bundoran.json
- [ ] **doing** · 2026-08-23 · OCR search sticks only after a term is typed. Leave open until verified on live Derry text bulletin · harvester/site_chrome.py, docs/assets/pdf-inpage-viewer.js
- [ ] **doing** · 2026-08-23 · Homepage cards: drop reliability words; keep coloured dots; small semibold ⚪ Bulletins ready @ 16:00 plus real this-week N/M from `build_diocese_week_summary()` / `parish_status` (leave —/— only when total is 0). Colour the dot from ready/total. **Leave open until live** https://www.parishpress.ie/ shows N/M not —/—. · harvester/site_builder.py `_live_card_ready_html`, `run()`
- [ ] **doing** · 2026-08-23 · Faithful searchable OCR: prefer each PDF's own text layer (PyMuPDF + PyPDF2) on born-digital pages so vision cannot drop notices or turn 22nd → 2nd. fill_sparse only helped banner-only pages; `_ORDINAL_DUP_RE` ate `22nd`; parish-name banner swallow dropped long notices; `_is_url_only_line` deleted wrap leftovers (`recently.`); Windows NUL/lock writes skipped dioceses; `extract_ocr_fragment` missed standalone `.ocr-body`. Vision stays one pass per diocese (max ~26) then split — no second vision API. Leave open until verified on generated parish/diocese HTML **and** live parishpress.ie · `ocr/text_extract.py`, `ocr/convert_bulletin.py`, `ocr/sparse_page_ocr.py`, `ocr/bulletin_layout.py`, `ocr/parish_pages.py`, `ocr/generate_bulletin_pages.py`, `ocr/fidelity.py`
- [x] **done** · 2026-08-24 · Image bulletin OCR was smashed (Annagry: `gi Teach`, mixed columns) and the standalone Annagry page was blank. Three causes: Annagry separates its contacts sidebar with a **printed vertical rule**, so the empty-valley gutter scan saw ink and read one column; the repair was then rejected because clean text still tripped `ocr_lines_look_smashed` on printed ordinals (`22™Aug`); and the OCR workflow stages mega PDFs in a temp dir, so `*.pages.json` was never found and Annagry's image-only banner page matched no name. Fix: `column_rule_xs()` finds printed rules, `column_smash_score()` / `replacement_is_better()` accept a repair that is *better* rather than perfect, strips inset past the gutter (kills `t|` / `»|` prefixes), `_is_scan_speckle()` drops artwork crumbs, and `load_mega_page_index()` also accepts a byte-identical `docs/mega_pdf/` twin. **Live-proved 24/08/2026** after PR [#120](https://github.com/Raphoe-Diocese/parish_harvester/pull/120) merge `2f44a197` + Pages run [32674384170](https://github.com/Raphoe-Diocese/parish_harvester/actions/runs/32674384170) success. Before (live): `Email: pp.annagry@gmail.com Wed 26 Aug 7.00pm Mary T. o Donnell` and standalone page `<div class="ocr-body" id="ocr-text"></div>` (empty). After, https://www.parishpress.ie/bulletins/raphoe-2026-08-23-ocr.html and https://www.parishpress.ie/parishes/raphoe/annagryparish-ocr.html: sidebar reads down whole (`Email: pp.annagry@gmail.com` / `Facebook: @annagryparish` / `Charity No. CHY7373`) and Mass times read across (`Wed 26""Aug 7.00pm Mary T. O'Donnell, Braade`). `gi Teach` / `Té Teach` absent. Irish kept: `Cuimhneacháin báis & bás le gairid`, `Loch an Iúir`, `Clár Empower Cumasú`. Annagry's PDF slice went from a 1.7 KB placeholder to 402,912 bytes. · `ocr/sparse_page_ocr.py`, `ocr/parish_pages.py`, `ocr/bulletin_layout.py`
- [x] **done** · 2026-08-24 · OCR parish headers sat in the wrong places, so text read under the wrong parish — Frank's screenshot: clicking **T** on https://www.parishpress.ie/dioceses/raphoe/ landed on `Tawnawilly / 23/08/2026 / Alpha / The Alpha is a 10 -week series of sessions`. OCR was reading the PDF fine; headers were located by matching the *printed* parish name, which is not a section boundary. Tawnawilly is mega pages 26–27; page 26 is its Mass times and only page 27 prints the name, so the header sat on Alpha. Killybegs (page 19) is named in a page 3 notice and headed the panel there. Dunfanaghy (page 9) never prints its name and had no header. Fix: `structure_ocr_html(parish_page_spans=…)` from the stitcher `*.pages.json` — header goes on the assigned page, a name printed outside its span stays body text, parishes absent from the index (Kilbarron, directory page) still match by name, and index/evidence spellings are reconciled so `Drumholm` + `Drumholm (Ballintra)` share one header. **Live-proved 24/08/2026** after PR [#124](https://github.com/Raphoe-Diocese/parish_harvester/pull/124) merge `a91a0e6e` + Pages run [32676544944](https://github.com/Raphoe-Diocese/parish_harvester/actions/runs/32676544944) success. After (live): `Tawnawilly / 23/08/2026 / Mass Times Intentions for this Week / Saturday 22nd Aug 6.00 pm Danny & Betty Meehan` — matches mega PDF page 26 (`Mass Times Intentions for this Week`). 22 headers in true page order, Dunfanaghy restored, Killybegs now 15th not 3rd. No Mass times, names or headings invented. · `ocr/bulletin_layout.py`, `ocr/generate_bulletin_pages.py`
- [ ] **doing** · 2026-08-23 · Welcome honesty: homepage says Parish Press is an ongoing project and searchable text may be incomplete — confirm Mass times, names, and notices against the original PDF. **Leave open until live** https://www.parishpress.ie/ · `harvester/site_builder.py` `_landing_page`, `docs/index.html`
- [x] **done** · 2026-08-23 · Put Ballycastle’s 23/08 bulletin (Enda / Edna Hill, printed spelling kept) on the public Parish Press pages. Live-checked 23/08/2026: https://www.parishpress.ie/parishes/down_and_connor/ballycastleparish-ocr.html HTTP 200, `Last-Modified: Sun, 23 Aug 2026 21:27:12 GMT`, title `Ballycastle Text Bulletin — 23/08/2026`. Exact OCR strings: `Enda Hill, R.I.P.` and `The family of the late Edna Hill` (printed spelling kept; `Enda Hill` once, `Edna Hill` once). · `docs/parishes/down_and_connor/ballycastleparish.pdf`, `docs/mega_pdf/down_and_connor_mega_bulletin.pdf` pages 5–6, `docs/bulletins/down_and_connor-*.html`
- [ ] **doing** · 2026-08-23 · Recipe success — 13 Problems-tab parishes. **Clonleigh / Ballymoney / Glenariffe / Kincasslagh live ok**. **Clones harvest ok** (not live on parishpress): `parish_status` `outcome=ok`, url `https://www.clonesparish.com/uploads/downloads/Sunday%2023rd%20August%202026.pdf`. **Castleblayney done** (harvest ok, not live on parishpress): harvest [32671059369](https://github.com/Raphoe-Diocese/parish_harvester/actions/runs/32671059369) wrote `parish_status` `outcome=ok`, url `http://mucknoparish.ie/wp-content/uploads/2020/09/F-Clontibret-Muckno-Bulletin-23rd-AUG-2026.pdf` (`last_tested_at` 2026-08-23T22:36:52+00:00, `error` null). Recipe merge PR #107 SHA `d6cea9d1`. That harvest run’s tests failed, but harvest still ran and committed. **Ballinascreen done** (harvest ok, not live on parishpress): harvest [32670756157](https://github.com/Raphoe-Diocese/parish_harvester/actions/runs/32670756157) wrote `parish_status` `outcome=ok`, url `https://www.parishofballinascreen.com/ballinascreen-desertmartin-parishes-23_august_2026`. This-week HTML page is real (proved 23/08/2026) HTTP 200 `text/html` title `BALLINASCREEN & DESERTMARTIN PARISHES 23_AUGUST_2026`; recipe is wix_dated_slug + print_to_pdf (PR #104 SHA `78063f9859f254cc0d2f5837998cc6f8a0fa0b15`). **Derriaghy this-week image found 25/08/2026** (recipe only — **not live yet**, no harvest): `21st-Suday-in-ordinary-time.png` HTTP 200, liturgical 23/08/2026; recipe switched to `http_scrape_newest_images`. Content gap still: Bangor 14/06, Dunsford 05/07, Holy Cross (`230826.pdf` byte-identical to `120726.pdf`), Hannahstown 07/06, Saint Malachy’s Feb 2025, St Oliver 28/06, St Patrick’s 05/07, Stranorlar 28/06, Bundoran 09/02/2025, Dromore 14/06 image, Saint Anthony 09/08. Ederney HTML masthead is 23/08 but harvest 403 — recipe already print_to_pdf. Skip Carrick / Lisburn / Tyholland. · `parishes/recipes/`, `harvester/replay.py`, `harvester/utils.py` `quote_http_url`
- [x] **done** · 2026-08-23 · Clones this-week file is **real** (re-proved 23/08/2026): encoded URL HTTP 200, `application/pdf`, 417727 bytes, `%PDF-1.7`. Raw space raised `InvalidURL`. Encoded `%20` in HTTP-scrape (no pinned filename). Cloud single-parish harvest wrote `parish_status` clones `outcome=ok`, url `https://www.clonesparish.com/uploads/downloads/Sunday%2023rd%20August%202026.pdf` (`last_tested_at` 23/08/2026 22:23 UTC). GitHub Actions `workflow_dispatch` returned 403 (token has no workflow scope). · `parishes/recipes/clogher/clones.json`, `harvester/utils.py` `quote_http_url`, `harvester/replay.py`
- [x] **done** · 2026-08-23 · Back to Top arrow (Divi-style ↑) shows when the reader scrolls the INNER PDF or OCR box, not only when the whole page scrolls. Live Raphoe already had capture-on-document JS, but the script ran before `#scroll-top-btn` and never bound the 850px boxes themselves. Fix: wait for the button / create it; bind `.pdf-inpage-pages` + `#ocr-panel` directly; re-bind after PDF.js rebuilds pages (`inner-2`, `?v=20260823u`). Live-proved 23/08/2026 on https://www.parishpress.ie/dioceses/raphoe/ after PR #109 merge `608987c1` + CI [32670814404](https://github.com/Raphoe-Diocese/parish_harvester/actions/runs/32670814404/job/97271281856) success. Live JS `pdf-inpage-viewer.js?v=20260823u` (`inner-2`), `Last-Modified: Sun, 23 Aug 2026 22:35:24 GMT`. Watched: arrow hidden at top (page 0, OCR 0, PDF 0); OCR inner scroll 240 → arrow on, click → OCR 0; PDF inner scroll 240 → arrow on, click → PDF 0. Boxes ~850px. · `harvester/site_chrome.py` `scroll_top_js`, `docs/assets/pdf-inpage-viewer.js`
- [x] **done** · 2026-08-23 · Desktop PDF + OCR boxes were **450px, not 850px, on a real desktop**. Frank was right and every earlier "we watched 850" check was measured in a wide window. Measured live this turn on https://www.parishpress.ie/dioceses/raphoe/ with headless Chrome: 1440px wide → PDF 850 / OCR 850, but **960px wide → PDF 450 / OCR 450** (`getComputedStyle().height` = `450px`). Cause: the 450px lock is width-only (`@media (max-width: 1024px)`, `!important` in `ensureStyles()`), and a half-screen window, Windows display scaling and browser zoom all report a CSS width under 1025px, so a mouse reader got the phone box. Fix: `desktop_viewer_height_lock_css()` — `@media (min-width: 701px) and (min-height: 501px)` re-locks 850px with `!important`, emitted after the 450 block in `pdf_inpage_viewer_css()` and in `ensureStyles()` (`?v=20260823v`), and patched into the four generated diocese pages. Phones keep 450px in both orientations (portrait 390×844, landscape 844×390 and 932×430 all measured 450). **Live-proved 23/08/2026** after PR [#121](https://github.com/Raphoe-Diocese/parish_harvester/pull/121) merge `01809d6b` + Pages run [32674571497](https://github.com/Raphoe-Diocese/parish_harvester/actions/runs/32674571497) success (live HTML `Last-Modified: Sun, 23 Aug 2026 23:46:21 GMT`, live JS `?v=20260823v` 23:46:22 GMT). Measured on https://www.parishpress.ie/dioceses/raphoe/ — 1440×1000, 1280×1000, 1100×1000, 1025×1000, **960×1040** and **1366×600** all give PDF `.pdf-inpage-pages` and OCR `#ocr-panel` rect **850px** with computed `height/min-height/max-height = 850px/850px/850px`; 844×390 and 390×844 still 450px. Derry, Down & Connor and Clogher diocese pages also measured 850/850 at 960px. · `ocr/generate_bulletin_pages.py` `desktop_viewer_height_lock_css`, `docs/assets/pdf-inpage-viewer.js`, `docs/dioceses/*/index.html`
- [ ] **doing** · 2026-08-23 · Ballintra IS Drumholm. Do not list Ballintra as missing/Facebook-only when Drumholm has this week’s PDF. One A–Z name: Drumholm (Ballintra). **Leave open until live** Raphoe generated HTML shows the combined name. · `parishes/recipes/raphoe/ballintra.json`, stitcher missing page, parish grid
- [x] **done** · 2026-08-23 · Diocese page intro: welcome; honest count; Facebook/website/stale lists. Heading must not say they “do not publish”. Every named parish gets a useful reader link only if already proved (recipe / status / live fetch this turn) — Facebook first (Mevagh, Gweedore `paroisteghaothdobhair`, Kilbarron recipe), else the parish website, else a proved official diocese parish page, else name-only. Live-checked 23/08/2026 on https://www.parishpress.ie/dioceses/raphoe/ after PR #106 merge `03d3c937` + Pages run [32671015074](https://github.com/Raphoe-Diocese/parish_harvester/actions/runs/32671015074) (`Last-Modified: Sun, 23 Aug 2026 22:35:24 GMT`). Exact heading: `Parishes that share news on Facebook or their website`. Gweedore is `https://www.facebook.com/paroisteghaothdobhair`. Old “do not publish” heading is gone. · `harvester/diocese_intro.py`, `parishes/recipes/raphoe/gaoth-dobhair.json`
- [x] **done** · 2026-08-23 · A–Z jump at the top of the PDF and OCR viewers. The first version (PR #95) rendered as a **tall grouped list** — a heading plus a `<ul>` of names per letter above each box, so the reader scrolled ~26 lines before reaching the bulletin. Replaced with **one compact letter row** (`az-row` / `az-letter`), only letters that have a parish; the letter jumps to that letter's first parish INSIDE the locked box. Each letter carries every parish under it (`data-az-names`) because a parish can be in the OCR text but absent from the collated PDF — Raphoe's Mevagh is in neither this week, so `M` falls through to Milford Kilkeel. Also fixed: the PDF half of #95 never worked — the A–Z script runs before the `#parish-page-index` JSON later in the body exists, so the index was always empty and every PDF jump silently fell through; it is now read lazily on first use. Mobile keeps 450px collapsed with a `Tap to enlarge` button that grows **only the tapped panel** to 850px (rules scoped to `#panel-pdf` / `#panel-ocr`, so a phone never stacks two 850px boxes). Desktop stays 850px; Back to Top (#109) intact and re-measured after expand. **Live-proved 23/08/2026** on https://www.parishpress.ie/dioceses/raphoe/ after PR #116 merge `1a7ab684` + Pages run [32673339047](https://github.com/Raphoe-Diocese/parish_harvester/actions/runs/32673339047) success. Live HTML has 0 × `az-jump-group` / `az-jump-link` / `az-jump-letters` / `az-jump-label` and 2 × `class="az-row"`; live PDF row is `A B C D F G I K M N R S T`. Watched in a browser on the live page: OCR `T` → Tawnawilly 8px from the top of the box (scrollTop 0 → 81973); PDF `T` → Tawnawilly's page 26, 8px from the top (scrollTop 0 → 22943); PDF `M` → Milford Kilkeel page 20; desktop both boxes 850px; phone 390px both 450px, tap OCR enlarge → OCR 850px with PDF still 450px. Four generated diocese pages regenerated (raphoe, derry, down-and-connor, clogher); rest of `docs/` untouched. · `ocr/generate_bulletin_pages.py` `render_az_jump_html` / `az_jump_css` / `az_jump_js`, `docs/assets/pdf-inpage-viewer.js` `ensureStyles`, `docs/dioceses/*/index.html`
- [x] **done** · 2026-08-24 · Frank: “where is the search bar gone!!!”, “I do like the new bar it works but you need to adjust it”, OCR `T` marked not working, snapshot strip chrome. **Reproduced his exact screenshot headless at 1024×461** (`max-width: 1024px` matches, `min-height: 501px` does not, so `Tap to enlarge` shows and boxes are 450px): the OCR block put the letter row, the *Open text in new tab* link and a full-width text-size bar **above** the search box, so search was the fourth row and off screen. OCR `T` did move the box to Tawnawilly at 1280×900 and 390×844 on live — but silently, inside a locked box, with the page overshooting, so there was nothing to see. Fixes: (1) search bar is **first** in the OCR block with a `SEARCH` label, styled like the JUMP TO bar; (2) letters + text size share **one** `.ocr-controls-row` bar (`margin-left: auto` on the zoom group) so neither hides the other and a narrow window no longer orphans a letter on its own line — phones stack them in one column and Prev/Next get a gap; (3) OCR letter jump is deterministic — the box scrolls **instantly** (two smooth scrolls on one chain were cancelling each other), then the page, then the offset is re-asserted for ~1s against reflow; falls back to `#ocr-parish-<slug>` and retries like the PDF jump because clearing a search rebuilds `#ocr-panel`; the parish it landed on flashes `.az-landed`; (4) `showBox` leaves room for the sticky search instead of hiding the header it just jumped to; (5) **snapshot strip**: PDF slots were sized from the full box width *before* the vertical scrollbar appeared, so every page was ~16px too wide inside the locked box — that is the horizontal scrollbar under Frank's strip — now `scrollbar-gutter: stable` + `.pdf-inpage-page-slot { max-width: 100% }` in both the generator and `ensureStyles()` (`?v=20260824a`). Derry had a second cause: **one** `.pdf-annot-link` whose PDF rect lands ~2100px off the right edge of its page stretched `.pdf-inpage-pages` `scrollWidth` to 2988 on its own — fixed with `.pdf-link-layer { overflow: hidden }` plus `.pdf-inpage-pages { overflow-x: hidden !important }` inside **both** locked media blocks, because their `overflow: auto !important` resets the horizontal axis. All four dioceses now measure `scrollWidth == clientWidth == 877` with `overflow-x: hidden`. No OCR text touched: the OCR fragment is byte-identical on all four regenerated diocese pages. Verified on generated `docs/dioceses/{raphoe,derry,down-and-connor,clogher}/index.html` at 1440×900, 1024×461 and 390×844 — OCR `T` lands Tawnawilly 8px from the top of the box and in the viewport, PDF `T` still lands mega page 26 8px from the top, boxes 850/450, `scrollWidth == clientWidth` on the PDF box, search visible above the letters, sticky on a typed term, 1/1 matches, no console errors. **Live-proved 24/08/2026** on https://www.parishpress.ie/dioceses/raphoe/ after PR [#125](https://github.com/Raphoe-Diocese/parish_harvester/pull/125) merge `85b9d904` + Pages run [32677394815](https://github.com/Raphoe-Diocese/parish_harvester/actions/runs/32677394815) success (live HTML `Last-Modified: Mon, 24 Aug 2026 00:40:50 GMT`, live JS `?v=20260824a` same stamp, 4 × `scrollbar-gutter:stable` / `overflow-x:hidden!important`). Inside `<div id="panel-ocr">` the live offsets are `ocr-search-label` 118, `id="ocr-search"` 183, `ocr-controls-row` 853, `az-row` 892, `ocr-zoom-bar` 3358, *Open text in new tab* 3974 — search is first. Clicked on live at **1024×461** (Frank's window), **1440×900** and **390×844**: OCR `T` → Tawnawilly masthead `topInBox` 8, in the viewport, `az-landed` flash present on the click and gone by 2.6s; PDF `T` → mega page 26 `topInBox` 8, in the viewport; boxes 848/850 desktop and 448/450 mobile; PDF box `scrollWidth == clientWidth`; typing “Tawnawilly” gives `1 / 1 matches` with the chrome `position: sticky`; no console errors. · `ocr/generate_bulletin_pages.py` (`render_bulletin_viewer_shell`, `az_jump_css`, `az_jump_js`, `pdf_inpage_viewer_css`), `docs/assets/pdf-inpage-viewer.js` (`ensureStyles`, `ensureStickySearch`), `docs/assets/pdf-mobile-fallback.js`, `docs/dioceses/*/index.html`
- [ ] **todo** · 2026-08-20 · parishpress.ie — live DNS / Pages cutover when Frank is ready · `docs/PARISHPRESS_IE_MIGRATION.md`, `docs/DOMAIN_SETUP.md`
- [ ] **todo** · 2026-08-20 · Site look — modern pages **after** OCR/capture/recipes are reliable · `harvester/site_builder.py`, `docs/`
- [ ] **parked** · 2026-08-23 · Longer diocese story — only if Frank asks again. No invented phones or emails.
- [x] **done — live-proved 2026-08-24** · **This week only: stop publishing old bulletin weeks.** `docs/bulletins/` is the only folder on the site that piles up — 182 files / 19 MB on `origin/main` `78063f98`, of which **12 are this week** (`*-2026-08-23*.html`, 4 dioceses × 3 pages) and **168 are old weeks back to 20/05/2026** (17 MB). Audit-only so far; nothing deleted.
  **Keep rule (do not break this):** keep `docs/bulletins/index.html` — `harvester/site_builder.py` line 1032 (`_ocr_standalone_url`) falls back to it when a diocese has no viewer page. Keep, **per diocese**, the newest dated trio `{diocese}-{date}.html` + `-ocr.html` + `-pdf.html`. Proof they are load-bearing: `docs/index.html` links `bulletins/{diocese}-2026-08-23-ocr.html` for all four dioceses; each `docs/dioceses/{diocese}/index.html` links `-ocr.html` **and** `-pdf.html`; `_latest_viewer` (line 431), `_latest_ocr_standalone` (line 462) and `_latest_pdf_standalone` (line 1046) pick the max date out of this folder; `harvester/manifest_builder.py` line 497 sets `ocr_viewer` from `{ocr_slug}-{target_date}.html`. Never touch `docs/parishes/` — its filenames carry no date, so it is already this-week-only.
  **Delete rule:** every older dated file (168). Verified no live page points at an old week — every dated `bulletins/…` reference in `docs/index.html`, all four diocese pages, `docs/subscribe/` and all 458 `docs/parishes/` pages is `2026-08-23`.
  **Order (two small PRs, N1 before N2):** (1) turn off the tap — add `prune_old_viewers()` to `ocr/generate_bulletin_pages.py`, called from `rebuild_indexes()`, keeping the newest date per diocese, because `ocr-bulletin.yml` writes `{diocese}-{TODAY}.html` + `-ocr` + `-pdf` and commits `git add docs` on every run, so a delete alone refills next week; (2) `git rm` the 168 already-published files and regenerate the index with `--rebuild-indexes`. Pages drops them because `deploy-pages.yml` builds `_site` from `cp -a docs/.`.
  **Accepted side effects, stated up front:** `_find_previous_viewer_path` (`ocr/generate_bulletin_pages.py` line 658) looks 7±3 days back in this folder, so `Bulletins/diffs/` becomes `no_prior_bulletin_found` — nothing reads that folder (38 of 188 already say it). `docs/search-index.json` shrinks from 3.72 MB to roughly 250 KB and `/search/` becomes this-week-only, which is the point. Old bookmarked `/bulletins/derry-2026-07-19.html` URLs will 404, which is also the point.
  **N1 status 23/08/2026: doing — PR [#115](https://github.com/Raphoe-Diocese/parish_harvester/pull/115) open, not merged, not live.** `prune_old_viewers()` added to `ocr/generate_bulletin_pages.py` and called from `rebuild_indexes()`: newest date per diocese kept, `index.html` / subfolders / unknown prefixes never touched, `--regenerate-from` protects the date it just rewrote, `BULLETIN_PRUNE_DISABLE=1` switches it off. 7 tests in `tests/test_bulletin_archive_prune.py`, including `test_current_week_ocr_and_pdf_links_survive_prune` (after a prune, `_latest_viewer` / `_latest_ocr_standalone` / `_latest_pdf_standalone` still resolve this week for every diocese). Dry run on a **copy** of the real folder: 168 old pages removed, this week's 12 + `index.html` + `docs/bulletins/raphoe/` kept. That PR regenerated and deleted **nothing** in `docs/` — the 168 are still committed and still live.
  **N1 is merged** — PR #115 merge `41c13064`, confirmed an ancestor of `origin/main` `1c7f7c60`.
  **N2 status 24/08/2026: done — PR [#127](https://github.com/Raphoe-Diocese/parish_harvester/pull/127) merged as `7fb20550`, Pages deploy [32678700149](https://github.com/Raphoe-Diocese/parish_harvester/actions/runs/32678700149) success, live-proved.** Ran the merged `prune_old_viewers()` itself (`python ocr/generate_bulletin_pages.py --rebuild-indexes`) rather than hand-picking a `git rm` list, so the delete rule and the keep rule are the same code. Re-verified on `1c7f7c60` before deleting: 60 diocese-date trios = 180 dated files, newest date `2026-08-23` for **all four** dioceses (derry 23 dates, down_and_connor 20, raphoe 16, clogher 1), so 56 old dates = **168 files**. Result: 168 deleted, 14 files left (12 this-week pages + `index.html` + `raphoe/index.html`), `docs/bulletins/index.html` rebuilt from 60 links to 4. `_latest_viewer` / `_latest_ocr_standalone` / `_latest_pdf_standalone` / `_ocr_standalone_url` all resolve `2026-08-23` for raphoe, derry, down_and_connor and clogher against the real pruned folder. Swept every `.html`/`.json`/`.xml`/`.js`/`.ics` file under `docs/`: 16 dated `bulletins/…` references, **0 broken**. 50 tests green (`test_bulletin_archive_prune`, `test_manifest_builder`, `test_landing_page`, `test_ocr_bulletin_pages`, `test_diocese_intro`, `test_raphoe_assets`). Old dates now survive only in markdown history (`docs/audit/2026-05-22-deep-audit.md`, `docs/EMBEDDING.md` — whose examples already pointed at the dead `frankytyrone.github.io` host) and in this backlog; those are not site nav, left alone.
  **Live proof 24/08/2026** (baseline taken first — all four old URLs below were HTTP **200** live before the merge):
  · **Old weeks gone.** HTTP **404**: https://www.parishpress.ie/bulletins/derry-2026-07-19.html · https://www.parishpress.ie/bulletins/down_and_connor-2026-05-20.html · https://www.parishpress.ie/bulletins/raphoe-2026-08-21-ocr.html · https://www.parishpress.ie/bulletins/derry-2026-05-22-pdf.html
  · **This week still works.** HTTP **200** on all 12 this-week pages, the homepage, `bulletins/`, and all four diocese pages. https://www.parishpress.ie/bulletins/raphoe-2026-08-23-ocr.html is 181,306 bytes, title `Raphoe Diocese Text Bulletin — 23/08/2026`, 24 `ocr-parish-name` headers, Irish kept as Irish (`AIFRINN NA SEACHTAINE`, `CUIMHNEACHÁIN`, `Gort a’ Choirce`, `POBAL CHRIOST`).
  · **Archive index reduced, not broken.** Live https://www.parishpress.ie/bulletins/ lists exactly **4** links, all `2026-08-23`. Live https://www.parishpress.ie/dioceses/raphoe/ points at `raphoe-2026-08-23-ocr.html` + `-pdf.html`. Live homepage links this week's four `-ocr.html`.
  · **Search not left broken.** https://www.parishpress.ie/search/ is 200 and live `search-index.json` is 3 documents, all `2026-08-23`, **0 dead links** (it would have been 999).
  Gameplan: `GAMEPLAN.md` 23/08/2026 items N1/N2 · `ocr/generate_bulletin_pages.py`, `docs/bulletins/`, `.github/workflows/ocr-bulletin.yml`
- [ ] **todo** · 2026-08-23 (updated 2026-08-24 by N2) · `docs/search-index.json` is stale and incomplete. It was generated 29/07/2026 with 999 documents across 12 May–July dates, Derry and Down & Connor only — and **every one of those 999 links pointed at a page N2 deletes**, so N2 had to rebuild it or leave `/search/` returning nothing but 404s. Rebuilt with the existing `_write_search_index`: now 3 live documents, all `2026-08-23`, derry + down_and_connor + clogher. Three causes still open, none fixed by N2: `harvest.yml` line 411 stages `docs/manifest.json docs/index.html docs/dioceses/` and never stages `docs/search-index.json`; `harvester/manifest_builder.py` line 17 `VIEWER_FILE_PATTERN` omits `raphoe`, so Raphoe has never entered the index; and line 22 `PARISH_NAME_PATTERN` still expects retired `<li class="parish-item">` markup, so the rebuild collapsed 95 parish-level documents to 3 diocese-level ones (the current pages use `ocr-parish-name` / `ocr-parish-masthead`). Same staleness on `docs/reliability.json` (30/07/2026, and `_build_reliability` reads a `recipes/learned/` folder that does not exist in the repo) and `docs/feeds/*.xml` (29/07/2026, no Clogher). Decide: fix and stage, or stop publishing `/search/`. **Leave open until live** https://www.parishpress.ie/search/ finds a this-week Raphoe notice · `harvester/manifest_builder.py`, `.github/workflows/harvest.yml`
- [ ] **todo** · 2026-08-23 · Diocesan calendars are empty shells — all 186 files in `Bulletins/events/` have `"events": []`, so `docs/calendars/*.ics` are 159–181 bytes and were last committed 30/07/2026. Either fix `harvester/events_extractor.py` or stop publishing the calendars. Do not leave a dead subscribe link on the site · `harvester/events_extractor.py`, `harvester/manifest_builder.py` `_write_ics_calendars`, `docs/calendars/`
- [ ] **todo** · 2026-08-23 · Dead homepage writer: `write_root_index()` in `ocr/generate_bulletin_pages.py` (lines 2569–2644) has no callers — `rebuild_indexes()` calls `write_bulletins_index()` only. If it ever ran it would overwrite the real homepage with the old "Parish Bulletin Dashboard" plus a "Browse the full OCR bulletin archive" link, which `tests/test_landing_page.py` line 140 asserts is **not** on the homepage. Delete the function; the live homepage stays `harvester/site_builder.py` `_landing_page` · `ocr/generate_bulletin_pages.py`
- [ ] **todo** · 2026-08-23 · Orphan pages, reachable only from `docs/sitemap.html` which nothing links to: `docs/badges/`, `docs/embed-examples.html`, `docs/search/`, `docs/calendars/`, `docs/feeds/`, `docs/audit/2026-05-22-deep-audit.md`. `docs/subscribe/` has no inbound link at all. Ask Frank before deleting any of these — default **keep**. The one clear delete: `docs/bulletins/raphoe/index.html`, a hand-written stub from 27/06/2026 with 24 hard-coded parish URLs, no generator, no inbound link, superseded by `docs/parishes/raphoe/` and `docs/dioceses/raphoe/` · `docs/sitemap.html`, `docs/bulletins/raphoe/index.html`
- [ ] **todo (NOW — audit 2026-08-23)** · **Live parish pages can show the wrong week, or an error page, while harvest says `ok`.** Proved live 23/08/2026, all three titled `23/08/2026` and all `outcome: ok` in `parishes/parish_status.json`: https://www.parishpress.ie/parishes/clogher/killanny-ocr.html shows May **2021** (`Pentecost Sunday`, `16th May`); https://www.parishpress.ie/parishes/raphoe/drive-1jmslbrliw-ocr.html shows `Sunday 19 July 2026 RAPHOE PARISH NEWSLETTER`; https://www.parishpress.ie/parishes/down_and_connor/sacredheartparishbelfast-ocr.html is a captured Apache **503 Service Unavailable** page. Not an OCR-quality bug — all three were OCR'd accurately, the pipeline published the wrong document. Causes and the small hardens are written up in `GAMEPLAN.md` 23/08/2026 brittleness audit as H1 (no body-date check when the URL has no date — 39 of 112 `ok` rows are `unknown:no_date_in_url`), H2 (`_ERROR_PAGE_PATTERN` has no 5xx wording) and H3 (8-day grace window accepts last Sunday — 8 parishes this week). Audit only; nothing implemented. **Leave open until live** those three URLs either show 23/08/2026 or are honestly marked stale · `harvester/bulletin_freshness.py`, `harvester/fetcher.py` `_recover_stale_bulletin`, `harvester/report.py` `_ERROR_PAGE_PATTERN`
- [ ] **todo** · 2026-08-23 · Public diocese grid publishes external links for `# DISABLED` parishes. `docs/dioceses/derry/index.html` has `<a class="parish-link" href="https://greencastleparish.com/" target="_blank" …>Greencastle</a>`; live 23/08/2026 that domain 302s to `http://ww80.greencastleparish.com/?subid1=…` → HTTP **502** (the parked/affiliate redirect its own evidence file flagged on 2026-08-10). Coleraine (St John) and Long Tower are the same shape (`parishes/derry_diocese_bulletin_urls.txt` lines 264–268, 287–294, 304–312). `# DISABLED` is read only by `harvester/parish_status.py` `_parse_disabled_keys`; the fetcher has **zero** references to it, so these are fetched every week. Small harden: in `render_parish_link_grid` do not fall back to an external href when the parish_status outcome is `disabled`. Overlaps the 23/08 "useful reader link only if already proved" row, which covered `harvester/diocese_intro.py` — this is the A–Z grid, a different generator. **Leave open until live** the Derry page no longer links a parked domain · `ocr/generate_bulletin_pages.py` `render_parish_link_grid` line 805, `harvester/fetcher.py` `parse_evidence_file`
- [ ] **todo** · 2026-08-24 · **8 of 116** published parish PDFs are ~1.7 KB placeholders reading *"PDF slice unavailable — This parish is marked OK, but its page range could not be found in this week's mega OCR"*. Was 12; the `pages.json` lookup fix (PR [#120](https://github.com/Raphoe-Diocese/parish_harvester/pull/120)) gave real slices to `annagryparish` (402,912 b), `iskaheenparish`, `fintona`, `ourladyqueenofpeacekilwee` and `newtownbutler`. Still placeholders: `castleblayney`, `clones`, `clontibret`, `tyholland`, `parishofballinascreen`, `kilmoreandkillyleagh`, `saintanthony`, `kincasslagh`. `parishofballinascreen` regressed from a 76,915 b slice at `783c2491` — it is absent from `derry_mega_bulletin.pages.json` **and** name-matching finds no range for it in this week's fragment, so the placeholder is the honest answer and the earlier slice was somebody else's pages. The placeholder wording is honest; the `ok` and the homepage ready count are not. Surface a `slice_missing` flag — do **not** touch the mega PDF for this, and do not redefine `ok`. `GAMEPLAN.md` H7 · `docs/parishes/`, `ocr/parish_splitter.py`, `harvester/parish_status.py`
- [ ] **parked** · 2026-07-30 · Remove the bulletin archive *page and nav* entirely — superseded by the "This week only" item above, which keeps `docs/bulletins/index.html` alive as the `site_builder` fallback. That landed live 24/08/2026 (N2, PR #127), so this is now revisitable — but still **parked until Frank asks**, and `docs/bulletins/index.html` cannot go while `_ocr_standalone_url` falls back to it · `AGENTS.md` parked list

---

## Harvest vs UI merge (do not panic)

A full harvest that **started** before a viewer-chrome merge (parish mastheads / 850px / new-tab) can commit and deploy `docs/dioceses/` from the old checkout and briefly overwrite live HTML. That is temporary.

`ocr-bulletin.yml` then checks out latest `main` (`ref: main`), runs `ocr/generate_bulletin_pages.py` (parish pages + dated viewers) and `harvester.site_builder.run()` (diocese `index.html` via `render_bulletin_viewer_shell` + `structure_ocr_html`), and restores the same files. Example: harvest [32384891762](https://github.com/Raphoe-Diocese/parish_harvester/actions/runs/32384891762) started 2026-08-20 15:14 UTC on `ac41d567` (no `ocr/bulletin_layout.py`); after it finishes, OCR on current main restores #52 chrome.

Harvest.yml also refreshes viewer templates from `origin/main` before writing the docs snapshot, so a later in-flight UI merge is less likely to clobber.

---

## Verify after every harvest regenerate

Harvest rewrites `docs/dioceses/*/index.html` from `render_bulletin_viewer_shell`. After a harvest or `harvester.site_builder.run()`, confirm:

```bash
python -m pytest tests/test_ocr_bulletin_pages.py tests/test_bulletin_layout.py tests/test_page_renderer.py tests/test_site_builder.py -q
```

Spot-check generated CSS for:

- `height: 850px`, `min-height: 850px`, and `max-height: 850px` on the visible `.pdf-inpage-pages` and `#ocr-panel` (not only the wrap)
- `overflow: auto` / `overflow-y: auto` on those boxes (content scrolls **inside** the 850px box)
- no `height: auto` and no `overflow: visible` on those visible boxes
- no `height: 85vh` clip on `.pdf-frame-wrap`
- `@media (max-width: 1024px)` with locked `height` / `min-height` / `max-height: 450px` and inner scroll on those same visible boxes
- `@media (min-width: 701px) and (min-height: 501px)` **after** that 450 block, re-locking 850px with `!important` (a desktop window narrower than 1025px — half-screen, Windows scaling, browser zoom — must not get the phone box)
- `.ocr-sticky-chrome` **outside** `#ocr-panel` is `position: relative` until `.is-searching` (typed term), then `position: sticky; top: 0`. `#scroll-top-btn` listens to **inner** `.pdf-inpage-pages` / `#ocr-panel` scroll as well as the page
- `#ocr-search` comes **before** `class="az-row"` and `class="ocr-zoom-bar"` inside `<div id="panel-ocr"`, and the letters + text size sit in one `.ocr-controls-row` (Frank could not find the search box under them)
- `scrollbar-gutter: stable` on `.pdf-inpage-pages` and `max-width: 100%` on `.pdf-inpage-page-slot`, or the first PDF page is sized before the scrollbar appears and the box grows a horizontal scrollbar
- Desktop lock keeps `.pdf-inpage-pages { overflow-x: hidden !important }`. Phone/tablet (`max-width: 1024px`) is `overflow-x: auto !important` so a 720px page can pan. `overflow: hidden` on `.pdf-link-layer` still clips a stray annotation (Derry: `scrollWidth` 2988)
- `/assets/pdf-inpage-viewer.js?v=20260827g` (cache-bust; Raphoe + Clogher diocese HTML pinned this turn — other dioceses still pin an older `?v=` but fetch the same `/assets/pdf-inpage-viewer.js` after cache)
- `ocr-parish-masthead` / `ocr-parish-name` in the OCR panel
- `getAnnotations` + `target="_blank"` in `docs/assets/pdf-inpage-viewer.js`
